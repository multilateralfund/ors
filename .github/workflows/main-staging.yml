name: "CI-staging"

on:
  push:
    branches:
      - "update-workflow-staging"

  workflow_call:

jobs:
  docker-build:
    uses: ./.github/workflows/docker-build.yml
  deploy-staging:
    needs: docker-build
    uses: ./.github/workflows/deploy-staging.yml
